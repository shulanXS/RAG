# =============================================================================
# Enterprise RAG System — Python 依赖清单
# =============================================================================
# Python >= 3.11
#
# 技术决策说明:
# - 不锁死 LLM 版本: openai SDK 兼容 OpenAI / DeepSeek（DeepSeek 走 OpenAI 协议），
#   openai>=1.12.0 提供 ChatCompletion + Embeddings + Stream 全套 API。
# - Qdrant-client: 选 Qdrant 而非 Chroma，因其 HNSW 优化、mmap 支持
#   和 sparse vector 原生混合搜索能力远强于 Chroma。
# - langgraph 保留（Phase1-1.1 删了 tool_node 后，状态机本身仍是 ReAct 的核心）
# =============================================================================

# --------------------------------------------------------------------------
# Document Processing / Parsing
# --------------------------------------------------------------------------
pypdf>=4.0.0
python-docx>=1.1.0
html2text>=2020.1.16
unstructured[pptx,pdf,docx,html]>=0.14.0

# --------------------------------------------------------------------------
# Embedding & Reranking
# --------------------------------------------------------------------------
# OpenAI Embedding（DeepSeek backend 也走 OpenAI 兼容协议，fallback 到 OpenAI Embedding）
openai>=1.12.0
# Cohere + BGE 双 reranker provider（用户选择 keep_both）
cohere>=5.5.0

# --------------------------------------------------------------------------
# Vector Database & Search
# --------------------------------------------------------------------------
qdrant-client>=1.9.0

# --------------------------------------------------------------------------
# Agentic Orchestration (LangGraph 状态机)
# --------------------------------------------------------------------------
# Phase1-1.1 删了 tool_node 后，langgraph 仍作为 ReAct 多步检索的状态机框架
langgraph>=0.2.0

# --------------------------------------------------------------------------
# Semantic Cache
# --------------------------------------------------------------------------
redis>=5.0.0
redisvl>=0.3.0

# --------------------------------------------------------------------------
# Evaluation (RAGAS only — Online Evaluator / DeepEval 已删除)
# --------------------------------------------------------------------------
ragas>=0.4.0
datasets>=2.14.0
numpy>=1.26.0

# --------------------------------------------------------------------------
# Config & Utilities
# --------------------------------------------------------------------------
pyyaml>=6.0.0
pydantic>=2.5.0
python-dotenv>=1.0.0
tiktoken>=0.7.0

# --------------------------------------------------------------------------
# Authentication
# --------------------------------------------------------------------------
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# --------------------------------------------------------------------------
# CLI & Interface
# --------------------------------------------------------------------------
typer>=0.12.0
rich>=13.7.0

# --------------------------------------------------------------------------
# Observability
# --------------------------------------------------------------------------
prometheus-client>=0.20.0
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-otlp-proto-grpc>=1.24.0
httpx>=0.27.0

# --------------------------------------------------------------------------
# Dev / Testing
# --------------------------------------------------------------------------
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# --------------------------------------------------------------------------
# Web API (FastAPI)
# --------------------------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# --------------------------------------------------------------------------
# Async Task Queue
# --------------------------------------------------------------------------
# Arq 轻量 async task queue，任务持久化到 Redis Stream。
# 替代之前的 BackgroundTasks（绑定 worker 进程，重启即丢）。
arq>=0.26.0
